<header class="w-full pt-6 flex justify-center relative">

  <!-- NAV ORIGINAL — solo desktop -->
  <nav
    class="hidden md:flex gap-10 px-10 py-3 
           rounded-2xl
           border border-white/70
           bg-black/40 backdrop-blur
           font-menu text-sm uppercase tracking-wider font-[800]"
  >
    <a href="" class="text-red-500 font-semibold">
      Inicio
    </a>

    <a href="#nosotros" class="text-white hover:text-gray-300">
      Nosotros
    </a>

    <a href="#label" class="text-white hover:text-gray-300">
      Fenix Eye Label
    </a>

    <a href="#tfk" class="text-white hover:text-gray-300">
      TFK House
    </a>

    <a href="#productions" class="text-white hover:text-gray-300">
      Fenix Eye Productions
    </a>

    <a href="#contacto" class="text-white hover:text-gray-300">
      Contacto
    </a>
  </nav>

  <!-- Barra mobile centrada -->
  <div class="md:hidden w-full max-w-md px-4 translate-y-6">
    <div class="flex items-center justify-center relative
                gap-6 px-6 py-3
                rounded-2xl
                border border-white/70
                bg-black/40 backdrop-blur
                font-menu text-sm uppercase tracking-wider font-[800]">

      <!-- Opciones visibles centradas -->
      <a href="" class="text-red-500 font-semibold">Inicio</a>
      <a href="#nosotros" class="text-white hover:text-gray-300">Nosotros</a>
      <a href="#label" class="text-white hover:text-gray-300 pr-5">Fenix Eye Label</a>

      <!-- Botón hamburguesa a la derecha -->
      <button
        id="menuBtn"
        class="absolute right-4 text-white text-xl pb-1.5"
        aria-label="Abrir menú"
      >
        ☰
      </button>
    </div>
  </div>

  <!-- Menú mobile desplegable (resto de opciones) -->
  <nav
    id="mobileMenu"
    class="hidden md:hidden absolute top-24 left-1/2 -translate-x-1/2
           w-[92%] max-w-md
           rounded-2xl
           border border-white/70
           bg-black/90 backdrop-blur
           font-menu text-sm uppercase tracking-wider font-[800]
           flex flex-col divide-y divide-white/20 z-40 text-center"
  >
    <a href="#tfk" class="p-4 text-white">TFK House</a>
    <a href="#productions" class="p-4 text-white">Fenix Eye Productions</a>
    <a href="#contacto" class="p-4 text-white">Contacto</a>
  </nav>

</header>

<script>
  const btn = document.getElementById('menuBtn')
  const menu = document.getElementById('mobileMenu')

  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden')
  })
</script>
